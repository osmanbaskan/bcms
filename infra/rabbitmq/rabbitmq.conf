# RabbitMQ configuration for BCMS
default_vhost = /
log.console = true
log.console.level = info

# Management plugin
management.tcp.port = 15672

# Memory / disk thresholds
vm_memory_high_watermark.relative = 0.6
disk_free_limit.absolute = 2GB

# Heartbeat
heartbeat = 60
